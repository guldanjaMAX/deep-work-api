// src/router.js
// Route dispatch table — fully wired in Task 5
// This shell establishes the module structure; imports added when index.js is thinned.

import { getCORSHeaders } from './utils/helpers.js';

// Route handler imports — added in Task 5

export async function routeRequest(request, env, ctx) {
  // Wired in Task 5 — this shell exists to establish the module structure
  throw new Error('Router not yet wired — Task 5 pending');
}
